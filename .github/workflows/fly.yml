name: Fly Deploy FastAPI service
on:
  push:
    branches:
      - main
jobs:
  deploy:
    name: Deploy app
    runs-on: ubuntu-latest
    # defaults:
    #   run:
    #     working-directory: ./fastapi-service/dockerdir_2
    steps:
    - uses: actions/checkout@v3
    - uses: actions/setup-python@v4
    - run: |
        python -m pip install --upgrade pip
        pip install --upgrade pip setuptools wheel
        pip install "mlem[streamlit,flyio]"
        mlem build docker_dir -m models/price_keras_preprocess --target dockerdir_2 --file_conf server=server.mlem
    # - uses: superfly/flyctl-actions/setup-flyctl@master
    # - name: Deploy to FLY IO
    # - run: flyctl deploy --remote-only -a art-expert-excited-newbeez-test
#          # flyctl scale memory 2048
      # env:
      #   FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}


# name: TelegramBot service
# on:
#   push:
#     branches:
#       - main
# jobs:
#   deploy:
#     name: Deploy app
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v3
#       - uses: superfly/flyctl-actions/setup-flyctl@master
#       - run: |
#           cd bot
#           flyctl deploy --remote-only
#           flyctl scale memory 512
#         env:
#           FLY_API_TOKEN: ${{ secrets.FLY_IO_API_TOKEN }}